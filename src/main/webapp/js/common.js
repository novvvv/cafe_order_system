// 공통 JavaScript 파일
console.log('웹 카페 프로젝트 JavaScript 로드됨');

// 공통 함수 예시
function showAlert(message) {
    alert(message);
}

// 페이지 로드 시 실행
document.addEventListener('DOMContentLoaded', function() {
    console.log('페이지 로드 완료');
});

